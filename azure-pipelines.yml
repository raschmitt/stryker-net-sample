trigger:
- master

pool:
  vmImage: 'windows-latest'

variables:
- group: stryker-net-sample

resources:
  repositories:
    - repository: templates
      type: git
      ref: refs/heads/master
      name: pipeline-templates

extends:
  # template: Build\template.yml@templates

  template: Tests\dotnet-mutation.yml@templates
  # parameters:
  #   strykerDashboardApiKey: $STRYKER_DASHBOARD_API_KEY