trigger:
- master

pool:
  vmImage: 'windows-latest'

resources:
  repositories:
    - repository: templates
      type: git
      ref: refs/heads/master
      name: pipeline-templates

extends:
  # template: Build\template.yml@templates

  template: Tests\dotnet-mutation.yml@templates